# Omega-Model RL Prototype

Вычислительный прототип **5D-циклической Omega-модели** — игрушечной математической формализации метафоры Omega-точки Тейяра де Шардена и Типлера.

**Важно:** это **игрушечная модель**, не физическая теория. «5D» — пространство состояния, «кваанты сознания» — математические сущности, а не частицы.

## Что это

Модель описывает **циклическую вселенную**:

1. **Big Bang** — из сингулярности разворачивается 5D-сфера.
2. **Expansion** — генерируется дерево ветвей (все возможные миры).
3. **Experience** — в каждой ветке симулируются «кваанты сознания».
4. **Omega Check** — проверяется глобальная когерентность.
5. **Collapse** — если когерентность выше порога, идеальная ветка сжимается в новую сингулярность.

**Когерентность** `C = |Σψ|² / Σ|ψ|²` — основная метрика.

## История версий

| Версия | Файл | Что нового | mC |
|--------|------|------------|-----|
| v3 | `code/omega_unified.py` | PPO baseline | 0.012 |
| v4 | `code/omega_recurrent.py` | + LSTM | 0.026 |
| v6 | `code/omega_branch_action.py` | + векторное действие | 0.026 |
| v9 | `v9-reward/omega_unified.py` | + reward, fix OMEGA_THRESHOLD | 0.356 |
| v10.5 | `v9-reward/omega_vector_v10_5.py` | + вектор `(3,)` + `TRAIN_STEPS=100000` | **0.389** |
| **v11** | `v9-reward/omega_evolution_v11.py` | **+ эволюция (рождение/смерть ветвей)** | **18.07** 🚀 |
| **v11.1** | `v9-reward/omega_evolution_v11_1.py` | **+ 10 сидов + MCTS baseline** | **18.32** 🚀 |

## v11: эволюция ветвей

**Идея:** ветви **рождаются** и **умирают** динамически:
- **`energy`** — растёт от когерентности, падает со временем.
- **`prune_branches`** — удаляет мёртвые ветви.
- **`spawn_branches`** — создаёт новые от root.
- **Reward** — популяционный (`alive / initial + C - S/12`).

**Результаты v11 (5 сидов, 4 условия):**

| Условие | R | mC |
|---------|---|-----|
| `random_evolution` | 300 | 11.27 |
| `trained_evolution` | 495 | **18.07** |
| `shuffled_evolution` | 50 | 12.74 |
| `constant_reward` | 0 | 0.09 |

**t-тесты:**
- `trained vs random`: **t = +4.10, p = 0.0011** ✅
- `trained vs shuffled`: **t = +10.33, p < 0.0001** ✅
- `trained vs constant`: **t = +11.40, p < 0.0001** ✅

## v11.1: 10 сидов + MCTS

**Добавлено:**
- **10 сидов** (было 5) — надёжная статистика.
- **MCTS** (Monte-Carlo Tree Search, 3 симуляции) — классический baseline.
- **`TRAIN_STEPS = 30000`** — компромисс.

**Результаты v11.1 (10 сидов, 5 условий):**

| Условие | R | mC |
|---------|---|-----|
| `constant_reward` | 0 | 0.085 |
| `random_evolution` | 300.5 | 10.63 |
| `shuffled_evolution` | 51.4 | 5.31 |
| `trained_evolution` (PPO) | 452.1 | **16.30** |
| **`mcts_evolution`** | **495.3** | **18.32** 🚀 |

**t-тесты:**
- `trained vs random`: t = +2.22, **p = 0.0534** (тренд)
- `trained vs shuffled`: **t = +6.69, p = 0.0001** ✅
- `trained vs constant`: **t = +7.59, p < 0.0001** ✅

## PPO vs MCTS

**MCTS (18.32) > PPO (16.30) на 12%.**

**Интерпретация:**
- **MCTS** — классический поиск, не требует обучения.
- **PPO** — нейросеть, требует 30000 шагов обучения.
- **MCTS** лучше в **маленькой** среде (40 ветвей).
- **PPO** в **100 раз быстрее** на инференсе (MCTS требует deepcopy).
- **PPO масштабируется** на большие среды, MCTS — нет.

**Вывод:** в малых средах классический поиск конкурентен; для больших сред нужны нейросети.

## Структура репозитория
